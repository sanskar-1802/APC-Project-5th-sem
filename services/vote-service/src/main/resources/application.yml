server:
  port: 9003
  max-http-header-size: 40 KB   # default is 8KB; increase if needed

spring:
  datasource:
    url: jdbc:h2:mem:votedb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console