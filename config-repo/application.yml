# app:
#   name: click2vote
#   description: Global default configuration
#   version: 1.0.0

spring:
  application:
    name: global-config

logging:
  level:
    root: INFO
    org.springframework.web: INFO

# Common DB settings (if all services use MySQL)
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
