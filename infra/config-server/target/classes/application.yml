

# server:
#   port: 8888   # Config Server runs on port 8888

# spring:
#   application:
#     name: config-server

#   cloud:
#     config:
#       server:
#         git:
#           uri: file:///C:/Users/Dell/Downloads/click2vote-starter/config-repo  # local repo path
#           default-label: main 
#           clone-on-start: true

server:
  port: 8888

spring:
  application:
    name: config-server

  profiles:
    active: native  

  cloud:
    config:
      server:
        native:
          search-locations: file:///C:/Users/Dell/Downloads/click2vote-starter-config-repo

